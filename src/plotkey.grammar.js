/* This javascript file will parse EBNF formal grammars and return tokens. */

var currentGrammar = {};

function setProductionRules (rules) {
  rules = rules.split(';\n');
}

function parseEBNF (phrase) {
  
}
